Independently review CR-TASK-260823-17qhsl-7 revision 7 against the boundary architecture decision and review verdict revision 6. Verify the six intentional blob changes from revision 6 and exact-match all 37 candidate paths and modes against the managed worktree. Reproduce source-built and freshly installed q JSON and compact attacks for sk-proj and opaque credential hostnames plus cookie session storage hostname and path components with mixed case separators percent encoding duplicate or malformed encodings. Require refusal or explicit unknown and absence from stdout stderr and cache. Narrow only hostname or path-component classification and require the named production tests with count one to fail. Re-run rev1-rev6 architecture attacks, full tests vet build format diff setup install parity task-board validation deterministic extractor pagination q grep m mutation cache target-origin and output-size gates. Inspect that hostname and every decoded path component flow through the single canonical normalized name and credential policy inside EnforceOutbound and that canonical URLs rescan to a clean fixed point. Accept only by creating an explicit accepted verdict outcome and invoking accept_cr for revision 7; otherwise record production evidence and route to-dev.