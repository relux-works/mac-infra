Rework CR revision 6 against TASK-260823-17qhsl_review-verdict-rev6.md without weakening TASK-260823-17qhsl_boundary-architecture-decision.md. The remaining gap is implementation-specific: recognized URLs are removed from the general remainder before decoded hostname and path components receive the canonical normalized name and credential policy. Keep one EnforceOutbound owner and no per-surface filters. Structurally decode and classify hostname plus every path component using the same sensitive-name policy as JSON keys assignments and query names, and scan every component for recognized credential and opaque-value forms. Preserve bounded normalization, fail closed on malformed or exhausted decoding, and rescan the canonical transformed URL to a clean fixed point. Add source-built and freshly installed q production tests in JSON and compact modes for sk-proj and opaque credential hostnames, cookie session storage host and path components, mixed case separators and encoded forms; assert refusal or explicit unknown and absence from stdout stderr and cache. Add a narrowing mutant removing only hostname or path classification and require the named production test with count one to fail. Re-run the complete architecture matrix, full tests vet build format diff setup install parity task-board validation deterministic extractor pagination q grep m mutation cache and output-size gates. Update task outcome and LOGBOOK, attach TASK-260823-17qhsl_rework-cycle5-validation-2026-08-24.md, and hand the next real CR to independent review. No human-only blocker exists.