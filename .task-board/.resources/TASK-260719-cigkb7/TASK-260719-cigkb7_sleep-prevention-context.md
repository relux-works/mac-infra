Evidence recovered on 2026-07-19: /usr/bin/pmset is canonical and modifying settings requires root. Current /usr/bin/pmset -g reports System-wide power settings: SleepDisabled 1, so prevention is already enabled on this Mac. pmset -g custom omits disablesleep and only shows ordinary AC/Battery timers (Battery sleep=1, AC sleep=0); therefore status must parse pmset -g as the source of truth and report the global enabled/disabled state plus applies_to AC,battery. Use only /usr/bin/pmset -a disablesleep 1 for enable and /usr/bin/pmset -a disablesleep 0 for disable through the existing root daemon. Do not rewrite sleep timers, standby, hibernatemode, display sleep, or other policy. Preserve unrelated dirty mac-audio-sweep changes in README.md, agents/skills/mac-infra/SKILL.md, go.mod, go.sum, scripts/setup.sh, scripts/deinit.sh, cmd/mac-audio-sweep, internal/audiodevice, internal/audiosweep, and EPIC-260702-3hw3he. Never reset, stage, or commit them.