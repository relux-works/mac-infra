Rework CR revision 1 finding F1 only. Preserve non-PII values in ambiguous public fields such as product name, addressType, and organization/application author; do not let field-name classification replace arbitrary values without value-level PII evidence. Add a named mac-browser-site production-entry regression across JSON, compact, cache, and grep plus a nearby true-PII positive control. Add a narrowing mutant restoring one overbroad generic-field classification and prove the named regression fails uncached, then restore bytes exactly. Update README, facade reference, implementation evidence, and LOGBOOK consistently. Re-run all required validation and publish revision 2 through handoff.