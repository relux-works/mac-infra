Review CR revision 1 as an immutable candidate. Independently attack: raw PII in compact and JSON q stdout; raw PII in cache and grep; placeholder stability and false-positive preservation; legacy or forged raw cache admission; invalid UTF-8 and malformed structured values; secret-shaped values still refusing atomically with no stdout/cache. Verify the production call sites and run a narrowing mutant or equivalent adversarial control. Record a task-scoped verdict resource and use accept_cr only if the exact revision and all gates pass.