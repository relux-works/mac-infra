# CR Revision 2 Focused Rereview

Review only the delta and evidence addressing prior blocking findings F1 and F2 from TASK-260823-zwtxq3_review-verdict.md. Verify that start and poll sources are captured separately, the start source alone pins origin/workspace guards before secret lookup and request start, and narrowing/removing those guards makes the named production-path test fail. Verify BlockedJavaScriptTokens against an independent literal expected set and literal fixtures, including cookiestore and opendatabase, and confirm dropping either token makes the test fail. Confirm the candidate was restored and focused/full tests remain green. Do not repeat the already completed broad 39-path mutation matrix, live heartbeat survey, or unrelated sibling-task review unless the rev2 delta invalidates that evidence. Accept CR revision 2 if these two findings are closed and no new regression appears in the focused delta.