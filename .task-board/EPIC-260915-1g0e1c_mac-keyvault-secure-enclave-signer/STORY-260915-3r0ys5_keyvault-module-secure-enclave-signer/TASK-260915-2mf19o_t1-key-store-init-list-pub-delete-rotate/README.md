# TASK-260915-2mf19o: t1-key-store-init-list-pub-delete-rotate

## Description
Key store commands over Security.framework (cgo-free where possible via the `security` CLI is NOT acceptable for the Enclave \u2014 use SecKey through a small Objective-C/cgo bridge or a Swift helper binary invoked by Go; decide and record why): `mac-keyvault init <label> [--enclave|--keychain]` creates a P-256 pair with kSecAttrIsPermanent, non-extractable, ACL kSecAccessControlPrivateKeyUsage bound to the user (biometry/passcode optional flag) and the calling binary; `list` (labels, store, created, public fingerprint SHA-256(DER(SPKI)) base64url); `pub <label> --out spki.der|pem`; `rotate <label>` (new pair under a versioned label, old kept until `delete`); `delete <label>` (explicit confirmation flag). JSON output mode. Negative tests: duplicate label refused; delete without confirmation refused; a label outside the tool's prefix is never deleted; Enclave-unavailable fallback is explicit, never silent.

## Scope
Rework of CR rev1 per review verdict rev1 (F1-F5) PLUS the CONSOLIDATED record model v2 from STORY resource STORY-260915-3r0ys5_key-record-model-v2.md (re-read it; it was rewritten on 2026-09-15 after your launch). Key points for this task: label is works.relux.mac-keyvault.<kind>.<service>.<purpose>.v<N> with .v<N> always present; uniqueness (kind, service, purpose, version); CLI address <service>/<purpose> with --kind (default key) and --version; schema-2 JSON record in kSecAttrApplicationTag with kind/service/purpose/version/title/description/algorithm/store/extraction/usages/format/created/origin/issuer/validity/meta; derived label/fingerprint/exposure/operations from a primitive registry holding exactly one row (key, ec-p256, keychain); init flags per §8 with --extraction none|human|agent as a literal flag (agent valid for key and secret, never a default); meta get/set/unset; describe; v1 tags read as schema 1, never upgraded. Keep the cgo bridge and the login-keychain store. T4-T7 own other kinds and the extraction ACL mechanics; this task validates and persists the fields (a key with extraction human/agent may still be non-extractable at the Keychain level in this task; T7 wires the ACL).

## Acceptance Criteria
Review findings: F1 foreign full label refused at the production run() entry with exit 3 and zero backend calls, plus a narrowing mutant; F2 duplicate refusal atomic across processes (exclusive create shape or a lock that a concurrent regression with positive control proves), plus mutant; F3 every usage error in --json mode yields a parseable error envelope on stdout, empty stderr, exit 2, no backend call; F4 rotate refuses when store/policy metadata is unknown, no keychain guess; F5 executable proof that the private key cannot be exported (SecItemExport/SecKeyCopyExternalRepresentation fails with the documented error) and that the SecAccess trusted-application set is exactly the calling binary, plus a narrowing ACL mutant. Model v2: record round-trips through init/list/describe/rotate; reserved names inside meta refused; invalid service/purpose/algorithm/usage/format/extraction refused before any Security call; describe prints operations for ec-p256 and [] with unsupported_primitive for an unknown row; go vet/build/test -count=1 ./... green; tests use works.relux.mac-keyvault.test. labels only; new Change Request revision published.
