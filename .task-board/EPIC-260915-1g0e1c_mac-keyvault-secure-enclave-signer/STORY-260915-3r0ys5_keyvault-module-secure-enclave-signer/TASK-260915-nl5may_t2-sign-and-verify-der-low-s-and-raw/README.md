# TASK-260915-nl5may: t2-sign-and-verify-der-low-s-and-raw

## Description
`mac-keyvault sign <label> --digest <hex|file> [--raw]` \u2014 ECDSA P-256 over a caller-supplied SHA-256 digest (the tool never hashes arbitrary files itself unless `--data` is given, then SHA-256 is explicit in the output), output strict DER with low-S normalisation (or raw r\u2016s 64 bytes with --raw); `verify --spki <der|pem>|--label <l> --digest --sig` returns exit 0/1 and JSON verdict; interop test: a signature made here verifies with Go crypto/ecdsa and with kvctl's verifier; negative tests: high-S signature refused by verify unless --allow-high-s; wrong digest length refused; tampered signature verdict false.

## Scope
Build on the checkpointed T1 candidate (story branch task-board/story/STORY-260915-3r0ys5, commit 1a150bd): mac-keyvault sign <service>/<purpose> [--kind key] [--version N] --digest <hex|@file> | --data-file F | --stdin [--raw] and verify --spki <der|pem> | <service>/<purpose> --digest ... --sig <hex|@file> [--raw] [--allow-high-s]. Follow the STORY resource STORY-260915-3r0ys5_key-record-model-v2.md: operations gate (usages must contain sign; validity.not_after unexpired -> expired code), format.signature default, error contract codes usage_refused/expired/... , registry-driven (add nothing outside the (key, ec-p256, keychain) row). Keep every T1 guard (address-only, prefix, JSON envelope on every path, no backend call before validation). Tests use works.relux.mac-keyvault.test. labels.

## Acceptance Criteria
sign over a 32-byte digest produces strict DER with low-S (verify with Go crypto/ecdsa and openssl); --raw produces 64-byte r||s; verify by label and by SPKI returns exit 0/1 with JSON verdict; interop: signature made here verifies with Go crypto/ecdsa and vice versa; negative: high-S refused by verify unless --allow-high-s, wrong digest length refused before any Security call, tampered signature verdict false, sign on usages without sign refused exit 3 (usage_refused), sign after not_after refused (expired), sign on a foreign address refused; describe operations reflect via: sign/verify instead of reserved; go vet/build/test -count=1 ./... green; Change Request published.
