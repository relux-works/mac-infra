# TASK-260610-2hwt3u: add-mac-infra-core-request-permissions

## Description
Add a narrow mac-infra-core command that prints the exact privileged helper binary/plist/socket targets and can open macOS Full Disk Access settings so the user can grant permissions to the core helper instead of broad launchers.

## Scope
(define task scope)

## Acceptance Criteria
(define acceptance criteria)
